definitions:
  domain.VentureReadModel:
    properties:
      description:
        type: string
      id:
        type: string
      imageUrl:
        type: string
      price:
        type: number
      status:
        type: string
      studentId:
        type: string
      title:
        type: string
    type: object
info:
  contact: {}
paths:
  /api/v1/catalog/ventures:
    get:
      consumes:
      - application/json
      description: Retrieve the full catalog of ventures (Read-Only)
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            items:
              $ref: '#/definitions/domain.VentureReadModel'
            type: array
        "500":
          description: Internal Server Error
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Get all ventures
      tags:
      - catalog
swagger: "2.0"
